# 练习 2: Channel 基础

## 🎯 学习目标

1. 理解 channel 是什么（goroutine 之间的管道）
2. 掌握无缓冲 vs 有缓冲 channel
3. 学会关闭和遍历 channel
4. 用 channel 实现 goroutine 通信

---

## 📝 核心概念

### Channel 是什么？

**Channel = Goroutine 之间传递数据的管道**

```
Goroutine A  →→→  [Channel]  →→→  Goroutine B
   (发送)                           (接收)
```

### 两种 Channel

| 类型 | 创建方式 | 特点 |
|------|---------|------|
| **无缓冲** | `make(chan int)` | 必须有人接收才能发送（阻塞） |
| **有缓冲** | `make(chan int, 3)` | 缓冲区未满可以直接发送 |

### 基本操作

```go
ch := make(chan int)     // 创建
ch <- 42                 // 发送
value := <-ch            // 接收
close(ch)                // 关闭
```

---

## 📖 练习说明

### 练习 1: 无缓冲 channel
- 创建 channel
- 一个 goroutine 发送，主函数接收

### 练习 2: 有缓冲 channel
- 创建容量为 3 的 channel
- 连续发送 3 个数字（不会阻塞）

### 练习 3: 关闭 channel
- 用 `close()` 关闭
- 用 `range` 遍历所有值

### 练习 4: 通信协作
- 生产者发送消息
- 消费者接收消息

---

## 🚀 运行

```bash
cd 02_channel_basic
go run main.go
```

---

## 💡 关键语法

### 创建
```go
ch1 := make(chan int)        // 无缓冲
ch2 := make(chan string, 5)  // 有缓冲，容量5
```

### 发送和接收
```go
ch <- value      // 发送（箭头指向 channel）
value := <-ch    // 接收（箭头从 channel 出来）
```

### 关闭和遍历
```go
close(ch)                    // 关闭

for value := range ch {      // 遍历直到关闭
    fmt.Println(value)
}
```

---

## ⚠️ 重要规则

1. **关闭后不能再发送**（会 panic）
2. **关闭后可以继续接收**（接收剩余的值，然后是零值）
3. **只有发送者关闭 channel**，接收者不要关闭
4. **无缓冲 channel 必须有接收者**，否则发送会永久阻塞

---

**完成后告诉我，我会检查和讲解！** 🚀
